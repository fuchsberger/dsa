<!DOCTYPE html>
<html lang="en">
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1"/>
    <meta name="author" content="Alexander Fuchsberger">
    <meta name="description" content="<%= gettext("A character, dice, and group management tool for DSA, Das Schwarze Auge") %>">
    <title><%= gettext("DSA Tool") %></title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
    <script type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </head>
  <body class='bg-gray-100'>
    <nav class="w-full bg-gray-800">
      <div class="relative mx-auto max-w-7xl px-3 h-12 flex items-center">
        <%= link gettext("DSA Tool"), to: Routes.session_path(@conn, :index), class: "brand" %>
      </div>
    </nav>
    <div class='container mx-auto p-3 pt-6 md:max-w-screen-sm text-center'>
      <h1 class='font-sans font-bold text-3xl'>Error 404</h1>
      <hr>
      <h2 class='font-sans font-medium text-xl mb-3'><%= get_flash(@conn, :error) || gettext "The server could not find the requested resource." %></h2>
      <%= link gettext("back to Home"), to: Routes.session_path(@conn, :index) %>
    </div>
  </body>
</html>
